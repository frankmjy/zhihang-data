# 内网保活迁移说明

## 用途

`scripts/intranet-keepalive.js` 是从当前风险/变更/演练本地服务里抽出来的独立保活脚本。它通过 Chrome/Edge 专用浏览器窗口复用人工登录后的 Cookie，定时访问内网页面，尽量避免 VPN 内网登录态因为长时间空闲而失效。

脚本不保存 VPN 账号、密码、验证码、Cookie 或 token。

## 本机使用

```powershell
npm run local:keepalive
```

或双击：

```text
start-intranet-keepalive.bat
```

首次运行时先连接 VPN，然后在脚本打开的 Chrome/Edge 窗口里完成内网登录。保活期间保持命令行窗口不要关闭。

## 迁移到其他 PC

1. 在新 PC 安装 Node.js 22 或更新版本。
2. 解压 `dist\intranet-keepalive-standalone.zip`。
3. 将 `.env.keepalive.example` 复制为 `.env.keepalive`，按需修改内网域名。
4. 连接 VPN。
5. 双击 `start-intranet-keepalive.bat`。
6. 在打开的浏览器窗口完成内网登录。

## 常用配置

- `INTRANET_KEEPALIVE_ORIGINS`: 要保活的内网域名，多个用英文逗号分隔。
- `INTRANET_KEEPALIVE_INTERVAL_MS`: 正常心跳间隔，默认 5 分钟。
- `INTRANET_KEEPALIVE_DEBUG_PORT`: 浏览器调试端口，默认 `9222`。
- `INTRANET_KEEPALIVE_PROFILE_DIR`: 专用浏览器用户数据目录。
- `INTRANET_KEEPALIVE_BROWSER_PATH`: Chrome/Edge 路径，留空自动查找。

## 自检命令

```powershell
node intranet-keepalive.js --print-config
node intranet-keepalive.js --once
```

`--once` 会执行一次真实心跳，需先连接 VPN 并完成内网登录。
